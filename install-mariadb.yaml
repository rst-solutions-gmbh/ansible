- name: Playbook to Install Packages
  hosts:
    - local
      #    - dev
      #    - prod
  tasks:
  become: true
  - name: Install mariadb
    package:
      name:
        - mariadb-server
      state: present
