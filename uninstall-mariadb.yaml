- name: Playbook to uninstall Packages
  hosts:
    - local
  become: yes
  become_method: sudo
  become_user: ralf
  vars:
    allow_world_readable_tmpfiles: true
  tasks:
  - name: Uninstall mariadb
    package:
      name:
        - mariadb-server
      state: absent
