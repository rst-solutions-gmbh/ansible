- name: Playbook to uninstall Packages
  hosts:
    - local
      #    - dev
      #    - prod
  #become: true
  #become_method: sudo
  tasks:
  - name: Uninstall mariadb
    package:
      name:
        - mariadb-server
      state: absent
